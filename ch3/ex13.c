#include <stdio.h>

int main () {
  int operando;
  int operador = 0;

  printf("Informe um número: \n");
  scanf("%d", &operando);

  printf("*** Tabuada de %d *** \n", operando);
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  operador++;
  printf("%d x %d = %d\n", operando, operador, operando * operador);
  
  return 0;
  }